# Core ML
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# XGBoost — champion model in this pipeline
xgboost>=2.0.0

# MLflow 3.x — required for alias-based registry (@champion/@challenger)
# MLflow 2.x used stage transitions which were removed in 3.x
mlflow==3.13.0

# Prefect 3.x orchestration
prefect>=3.0.0

# Parquet reading (TLC direct download)
pyarrow>=14.0.0
